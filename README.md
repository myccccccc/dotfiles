# Usage

```bash
cd ~ && git clone --recurse-submodules https://github.com/myccccccc/dotfiles.git
rm -rf .vimrc && ln -s dotfiles/.vimrc .vimrc
rm -rf .vim && ln -s dotfiles/.vim .vim
rm -rf .zshrc && ln -s dotfiles/.zshrc .zshrc
rm -rf .zsh && ln -s dotfiles/.zsh .zsh
```

